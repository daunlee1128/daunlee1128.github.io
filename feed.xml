---
permalink: /feed.xml
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html"/>
  <id>{{ '/' | absolute_url }}</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author><name>{{ site.handle | xml_escape }}</name></author>
  {%- assign all = site.tech | concat: site.insights | sort: "date" | reverse -%}
  {%- for p in all limit: 20 %}
  <entry>
    <title>{{ p.title | xml_escape }}</title>
    <link href="{{ p.url | absolute_url }}" rel="alternate" type="text/html"/>
    <id>{{ p.url | absolute_url }}</id>
    <published>{{ p.date | date_to_xmlschema }}</published>
    <updated>{{ p.date | date_to_xmlschema }}</updated>
    <category term="{{ p.collection }}"/>
    {%- if p.kind %}<category term="{{ p.kind }}"/>{% endif %}
    {%- for s in p.stack %}<category term="{{ s }}"/>{% endfor %}
    <summary>{{ p.summary | xml_escape }}</summary>
    <content type="html" xml:base="{{ p.url | absolute_url }}">{{ p.content | xml_escape }}</content>
  </entry>
  {%- endfor %}
</feed>
